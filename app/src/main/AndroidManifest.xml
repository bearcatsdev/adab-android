<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    package="com.ambinusian.adab">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.Adab"
        tools:ignore="GoogleAppIndexingWarning">
        <activity android:name=".ui.student.LatestClassActivity"></activity>
        <!-- IDK, but we need to work on Google App Index Warning -->
        <activity android:name=".ui.student.NextClassActivity" />
        <activity android:name=".ui.lecturer.ActivityLive" />
        <activity android:name=".ui.student.CourseDetail" />
        <activity android:name=".ui.lecturer.ActivityClassStarted" />
        <activity android:name=".ui.lecturer.CourseDetail" />
        <activity android:name=".ui.lecturer.MainActivity" />
        <activity android:name=".ui.student.ActivityLive" />
        <activity android:name=".ui.lecturer.NextScheduleActivity" />
        <activity
            android:name=".ui.splash.SplashActivity"
            android:theme="@style/Theme.Adab.Launcher">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".ui.login.ActivityLogin" />
        <activity android:name=".ui.student.MainActivity" />
    </application>
    <uses-permission android:name="android.permission.RECORD_AUDIO" />
    <uses-permission android:name="android.permission.INTERNET" />

</manifest>