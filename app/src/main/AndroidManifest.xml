<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.ambinusian.adab">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.Adab">
        <activity android:name=".all.NextClassActivity"></activity>
        <activity android:name=".ui.lecturer.LecturerLiveSessionActivity" />
        <activity android:name=".ui.student.CourseDetail" />
        <activity android:name=".ui.lecturer.ClassStartedActivity" />
        <activity android:name=".ui.lecturer.CourseDetail" />
        <activity android:name=".ui.lecturer.MainActivity" />
        <activity android:name=".ui.student.LiveSessionActivity" />
        <activity
            android:name=".ui.splash.SplashActivity"
            android:theme="@style/Theme.Adab.Launcher">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".ui.login.LoginActivity" />
        <activity android:name=".ui.student.MainActivity" />
    </application>

    <uses-permission android:name="android.permission.INTERNET" />

</manifest>